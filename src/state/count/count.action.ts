import { createAction, props } from "@ngrx/store";

export const countIncreasement = createAction(
    "[Count] Count Increasement"
)

export const countDecreasement = createAction(
    "[Count] Count Decreasement"

)